
struct BinaryNode
// this struct defines what the binary node is
{
    // each binary node contains an integer value, a pointer to the address of the node to the left of it and another one to the address to the right of it
    int value; 
    BinaryNode *left;
    BinaryNode *right;
};
